apiVersion: v1
kind: Service
metadata:
  name: {{ include "cluster-cronitor.fullname" . }}
  labels:
    {{- include "cluster-cronitor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cluster-cronitor.selectorLabels" . | nindent 4 }}
